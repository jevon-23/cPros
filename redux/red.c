//This allows us to print
#include <stdio.h>
#include <stdlib.h>

int main(int arg, char *argv[]) {
}
int processCLI(int argc, char **argv, char **filename) {
  if (argc != 2) {
    printf("usage:");
    exit(-1);
  }
  return 0;
}
